import chalk from 'chalk';

export async function showRecentConfluencePages(spaceKey: string, limit: number) {
  console.log(chalk.yellow(`showRecentConfluencePages ${spaceKey} limit=${limit} - Not yet implemented`));
}

export async function viewConfluencePage(pageId: string) {
  console.log(chalk.yellow(`viewConfluencePage ${pageId} - Not yet implemented`));
}
